<h2>Mascotas publicadas</h2>


<%= render "pets/petslist" %>
<!--

<table>

<% @pets.each do |pet| %>
  <tr>
    <td><%= pet.name %></td>
    <td><%= pet.creationDate %></td>
    <td><%= pet.lostDate %></td>
    <td><%= pet.foundDate %></td>
    <td><%= pet.adoptedDate %></td>
    <td><%= link_to 'Show', pet %></td>
    <td><%= link_to 'Edit', edit_pet_path(pet) %></td>
    <td><%= link_to 'Destroy', pet, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

-->
<br />
<%= link_to 'Agregar mascota', new_pet_path, :class =>"btn btn-success", :style => "color: white" %>
